"""
Urls related to our profile - update/upgrade etc.
"""
from django.conf.urls import patterns, url

from django.views.generic import TemplateView

urlpatterns = patterns(
    '',
#    url(r'^upgrade/advanced$', ),
    )
